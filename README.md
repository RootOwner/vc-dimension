#  README.md

# Norm-Based Complexity Measures and Generalization in Deep Neural Networks

Данный репозиторий содержит код, экспериментальные результаты и исходные файлы
курсовой научно-исследовательской работы, посвящённой анализу обобщающей
способности глубоких нейронных сетей на основе norm-based мер сложности,
маржинных оценок и спектральных норм весовых матриц.

Работа сочетает теоретический анализ (VC-теория, Rademacher complexity,
маржинные границы) и вычислительный эксперимент, подтверждающий применимость
norm-based прокси-оценки сложности
\[
\hat{\Psi}(\theta) = \prod_{\ell=1}^{L} \| W_\ell \|_2
\]
для сравнения моделей с одинаковым числом параметров.


||-----------------------------------------------------------------------------------------||

##  Структура репозитория

```

nir
├── code/                  # Экспериментальный код (Python)
│   ├── complexity.py      # Spectral norms и Ψ̂(θ)
│   ├── config.yaml        # Все параметры эксперимента
│   ├── datasets.py        # Загрузка и подготовка данных
│   ├── evaluate.py        # Accuracy и margin
│   ├── models.py          # Архитектуры нейросетей
│   ├── plots.py           # Генерация всех графиков
│   ├── run_all.py         # Главный автозапуск экспериментов
│   ├── train.py           # Обучение моделей
│   └── utils.py
├── figures
│   ├── accuracy_vs_psi.pdf
│   └── margin_vs_psi.pdf
├── main.tex
├── Makefile               # Автосборка PDF
├── README.md
├── references.bib
├── requirements.txt       # Python-зависимости
├── results/               # Результаты экспериментов
├── sections
└── tables




````

||-----------------------------------------------------------------------------------------||

##  Краткое описание эксперимента

В рамках эксперимента рассматриваются несколько нейронных сетей с одинаковым
числом параметров, обучаемых на стандартных датасетах (MNIST / CIFAR-10 subset).
Для каждой модели вычисляются:

- точность классификации (Accuracy);
- эмпирический маржин классификатора;
- произведение спектральных норм весовых матриц;
- norm-based прокси-оценка сложности $\hat{\Psi}(\theta)$.

Анализируются корреляции между качеством обобщения и norm-based характеристиками
сложности.

||-----------------------------------------------------------------------------------------||

##  Требования к среде

### Операционная система
- Ubuntu 20.04 / 22.04 (проверено)

### Системные зависимости

```

sudo apt update
sudo apt install -y python3 python3-pip python3-venv \
                    make


```



||-----------------------------------------------------------------------------------------||


##  Автозапуск экспериментов


Доступные цели:
  setup      : Создание виртуального окружения и установка зависимостей
  run        : Запуск полного эксперимента (обучение + оценка + графики) (запуск run_all.py)
  train      : Только обучение моделей
  evaluate   : Только оценка моделей
  plots      : Только генерация графиков
  pdf        : Сборка LaTeX-документа (main.pdf) (Установка зависимостей при необходимости)
  clean      : Удаление вспомогательных файлов LaTeX
  clean-all  : Удаление всех сгенерированных файлов
  help       : Показать это сообщение справки

Пример рабочего процесса:
  make setup
  make run
  make pdf

Системные требования (Ubuntu):
  sudo apt update && sudo apt install python3 python3-pip python3-venv make

|-----------------------------------------------------------------------------------------|

### Краткое описание команд Makefile

- `make setup` 
  Создаёт виртуальное окружение `venv/` и устанавливает все Python-зависимости из
  `requirements.txt`. Выполняется один раз перед запуском экспериментов.

- Объязательно нужно активировать вирутальное окружение перед запуском `make run`
  Для запуска виртуального окружение: source venv/bin/activate

- `make run` 
  Запускает полный эксперимент через `run_all.py`: обучение моделей, вычисление
  всех метрик и генерацию графиков. Использует Python из виртуального окружения.

- `make train`
  Выполняет только этап обучения моделей без последующей оценки и построения
  графиков.

- `make evaluate`
  Вычисляет accuracy, margin и norm-based метрики для уже обученных моделей.

- `make plots`
  Генерирует все графики на основе сохранённых результатов экспериментов.

- `make pdf`
  Компилирует LaTeX-документ `main.tex` в итоговый файл `main.pdf`. При необходимости
  автоматически устанавливает LaTeX-зависимости и `biber`.

- `make clean` 
  Удаляет временные файлы, создаваемые при компиляции LaTeX-документа.

- `make clean-all`
  Удаляет все сгенерированные результаты экспериментов и графики, а также
  вспомогательные файлы LaTeX.

- `make help`
  Выводит справочную информацию по всем доступным целям Makefile.

||-----------------------------------------------------------------------------------------||


```

Скрипт `run_all.py` автоматически:

1. фиксирует random seed;
2. загружает датасеты;
3. обучает все модели;
4. вычисляет accuracy, margin и $\hat{\Psi}(\theta)$;
5. сохраняет результаты в `results/`;
6. генерирует все графики в `figures/`.

📌 После выполнения скрипта все рисунки готовы к включению в LaTeX-документ.

||-----------------------------------------------------------------------------------------||

##  Генерируемые графики

* `accuracy_vs_psi.pdf` — зависимость Accuracy от $\hat{\Psi}(\theta)$
* `margin_vs_psi.pdf` — зависимость margin от $\hat{\Psi}(\theta)$
* дополнительные графики для Приложения

Все графики воспроизводимы и генерируются автоматически.

||-----------------------------------------------------------------------------------------||


##  Воспроизводимость

* Все эксперименты используют фиксированные random seed.
* Все параметры эксперимента зафиксированы в `config.yaml`.
* Все результаты можно воспроизвести одной командой.

||-----------------------------------------------------------------------------------------||

##  Связь с теорией

Реализованные метрики напрямую соответствуют теоретическим результатам,
рассмотренным в работе:

* маржинные оценки обобщающей ошибки;
* norm-based bounds;
* спектральные нормы и липшицевость нейронных сетей.

Эксперимент подтверждает, что norm-based характеристики сложности позволяют
лучше различать модели с близкой точностью классификации.

||-----------------------------------------------------------------------------------------||

##  Автор

Курсовая работа выполнена в рамках обучения в СПбГУ.
Автор: *[Бокиев Бахромжон Боходир угли]*
Научный руководитель: *[Мокаев Тимур Назирович]*



||-----------------------------------------------------------------------------------------||
# vc-dimension
# vc-dimension
